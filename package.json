{
  "name": "dd-softlab-design-lib-workspace",
  "private": true,
  "scripts": {
    "generate-theme-scss": "node ./scripts/generate-theme-scss.mjs",
    "prebuild:lib": "npm run generate-theme-scss",
    "build:lib": "ng build dd-softlab-design-lib --configuration production",
    "build": "npm run build:lib",
    "pack:lib": "npm run build:lib && npm pack ./dist/dd-softlab-design-lib",
    "publish:lib": "npm --prefix ./projects/dd-softlab-design-lib version patch --no-git-tag-version && npm run build:lib && npm publish ./dist/dd-softlab-design-lib --provenance"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.1.0",
    "@angular/common": "^21.1.0",
    "@angular/cli": "^21.1.0",
    "@angular/compiler-cli": "^21.1.0",
    "@angular/core": "^21.1.0",
    "@angular/router": "^21.1.0",
    "ng-packagr": "^21.1.0",
    "typescript": "~5.9.3"
  },
  "sideEffects": false
}
