{
  "name": "dd-softlab-design-lib-workspace",
  "private": true,
  "scripts": {
    "generate-theme-scss": "node ./scripts/generate-theme-scss.mjs",
    "prebuild:lib": "npm run generate-theme-scss",
    "build:lib": "ng build dd-softlab-design-lib --configuration production",
    "build:lib:ng-packagr": "npm run build:lib",
    "clean:lib": "node -e \"require('fs').rmSync('dist/dd-softlab-design-lib', { recursive: true, force: true });\"",
    "build:lib:clean": "npm run clean:lib && npm run build:lib",
    "build": "npm run build:lib:clean",
    "pack:lib": "npm run build:lib && npm pack ./dist/dd-softlab-design-lib"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "ng-packagr": "^21.0.0",
    "semantic-release": "^25.0.3",
    "typescript": "~5.9.3",
    "zone.js": "^0.16.0"
  },
  "sideEffects": false
}
